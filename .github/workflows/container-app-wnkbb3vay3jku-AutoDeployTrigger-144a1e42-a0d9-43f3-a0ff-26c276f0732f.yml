name: Trigger auto deployment for container-app-wnkbb3vay3jku

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-wnkbb3vay3jku-AutoDeployTrigger-144a1e42-a0d9-43f3-a0ff-26c276f0732f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPWNKBB3VAY3JKU_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPWNKBB3VAY3JKU_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPWNKBB3VAY3JKU_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPWNKBB3VAY3JKU_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPWNKBB3VAY3JKU_REGISTRY_PASSWORD }}
          containerAppName: container-app-wnkbb3vay3jku
          resourceGroup: flaskbasicbc0c9cef
          imageToBuild: default/container-app-wnkbb3vay3jku:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


